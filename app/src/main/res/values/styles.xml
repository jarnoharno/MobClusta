<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Set theme colors from http://www.google.com/design/spec/style/color.html#color-color-palette-->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="android:color">@color/color</item>
        <!--<item name="android:colorPrimary">@color/primary</item>-->
        <!-- colorPrimaryDark is used for the status bar -->
        <!--<item name="android:colorPrimaryDark">@color/primary_dark/item>-->
        <!-- colorAccent is used as the default value for colorControlActivated
             which is used to tint widgets -->
        <!--<item name="android:colorAccent">@color/accent</item>-->
        <!-- You can also set colorControlNormal, colorControlActivated
             colorControlHighlight and colorSwitchThumbNormal. -->
        <!--<item name="android:actionMenuTextColor">@color/color</item>
        <item name="android:colorControlActivated">@color/primary</item>
        <item name="android:indeterminateTintMode">src_atop</item>
        <item name="android:indeterminateTint">@color/primary_dark</item>
        <item name="android:tintMode">src_atop</item>-->
    </style>

    <style name="AppTheme.Toolbar" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">?android:color</item>
    </style>

    <style name="AppTheme.SubTitle">
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:textColor">?android:colorPrimaryDark</item>
    </style>

    <style name="AppTheme.SubTitle.Right">
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">16dp</item>
    </style>

    <style name="AppTheme.SubTitle.SubSubTitle">
        <!--<item name="android:textColor">?android:colorForeground</item>-->
    </style>

    <style name="AppTheme.SubTitle.SubSubTitle.Log">
        <item name="android:fontFamily">monospace</item>
        <item name="android:textSize">8dp</item>
    </style>

</resources>
