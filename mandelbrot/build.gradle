apply plugin: 'application'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'commons-cli:commons-cli:1.3.1'
    compile project(':libmandelbrot')
}

mainClassName = "fi.hiit.mandelbrot.Main"